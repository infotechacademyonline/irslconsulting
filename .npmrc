# Sanity's own dep tree pulls in @sveltejs/kit for their Studio dev tooling,
# which now conflicts with @vercel/analytics' optional peer. Both work fine at
# runtime; only npm's strict resolver objects. legacy-peer-deps=true lets the
# install complete with the conflicting peer resolved to the primary version.
legacy-peer-deps=true
fund=false
audit=false
